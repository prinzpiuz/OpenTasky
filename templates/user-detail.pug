extends layouts/base.pug

block content
    main.bg-white.mx-auto.mt-10.p-10.w-full(class="lg_w-4/12")
        h1 #{user.name}
        p Email: #{user.email}
        .mt-4
            a(href='/admin/edit/user/' + user.id + '/?edit=true').button.mr-4 Edit
            a(href='/admin/delete/user/' + user.id) Delete