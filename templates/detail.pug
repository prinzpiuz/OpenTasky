extends layouts/base.pug

block content
    main.bg-white.mx-auto.mt-10.p-10.w-full(class="lg_w-4/12")
        h1 #{task.user}
        p ID: #{task.id}
        p Task:  #{task.task}
        p Reference: #{task.reference}
        .mt-4
            a(href='/admin/edit/' + task.id + '/?edit=true').button.mr-4 Edit
            a(href='/admin/delete/' + task.id) Delete